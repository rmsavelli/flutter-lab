name: luklakmmap
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  intl: ^0.20.2
  open_file: ^3.5.10
  path_provider: ^2.1.5
  supabase_flutter: ^2.9.1
  syncfusion_flutter_pdf: ^30.1.41

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo-lukla.png
    - assets/empty_form.pdf
    - .env
